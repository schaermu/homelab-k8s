---
apiVersion: v1
kind: Service
metadata:
  name: mediamanager
  namespace: mediamanager
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
