---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-lab-schaermu
  namespace: gateway
spec:
  secretName: cert-lab-schaermu
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: cloudflare-cluster-issuer-staging
  dnsNames:
    - "*.lab.schaermu.ch"
    - "lab.schaermu.ch"

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-schaermu
  namespace: gateway
spec:
  secretName: cert-schaermu
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: cloudflare-cluster-issuer-staging
  dnsNames:
    - "*.schaermu.ch"
    - "schaermu.ch"
