apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-schaermu-ch
  namespace: traefik
spec:
  secretName: wildcard-schaermu-ch-tls
  dnsNames:
    - "lab.schaermu.ch"
    - "*.lab.schaermu.ch"
  issuerRef:
    name: cloudflare-issuer
    kind: Issuer
