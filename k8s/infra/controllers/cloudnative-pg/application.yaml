---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg-operator
  namespace: argocd
spec:
  project: controllers
  source:
    path: cnpg/cloudnative-pg
    repoURL: https://cloudnative-pg.github.io/charts
    targetRevision: cloudnative-pg-v0.27.0
  destination:
    name: in-cluster
    namespace: cnpg-system
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
