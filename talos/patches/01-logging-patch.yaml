machine:
  install:
    extraKernelArgs:
      - talos.logging.kernel=udp://127.0.0.1:6050/
  logging:
    destinations:
      - endpoint: "udp://127.0.0.1:6050/"
        format: "json_lines"
        extraTags:
          hostname: k8s-homelab-worker-3
